# Dockerfile Templates

Future production-oriented Dockerfile and multi-stage build templates. Each
addition must document build targets, runtime user, secret handling, image
validation and intended workload type.
