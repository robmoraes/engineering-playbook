# Reference Examples

Reusable samples that demonstrate the standards documented in this playbook.

## Planned Content

- `docker/` - reference Dockerfiles and Compose definitions.
- `github-actions/` - workflow examples.
- `swarm/` - stack and service deployment examples.
- `scripts/` - operational automation examples.

Examples should remain generic and must not contain production credentials or environment-specific secrets.
